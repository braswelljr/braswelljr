/**
 * isFalsy - Checks if a value is falsy. (null, undefined, NaN, 0, false, '')
 * @param value - The value to check.
 * @returns - Returns true if the value is falsy, otherwise false.
 */
export default function isFalsy(value: any): boolean {
  return !value
}
